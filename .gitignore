__pycache__/
*.pyc
config.json

# sqlite 索引是派生数据，随时可重建。写成 **/ 是因为数据目录可能被配置到仓库子目录，
# 而含斜杠的模式只锚定在本文件所在目录。
**/data/*.db
**/data/*.db-*

# 个人投递状态：每人各自的进度，不参与共享
local/

# CV 原文与解读：任何人的个人信息都不进 git。
# 本仓库是公开的，推上去等同于公开发布，而且会永久留在 git 历史里。
# 只保留 README.md（格式约定文档），其余一律挡掉。
cv/*
!cv/README.md

# 原子写留下的临时文件
*.tmp
.DS_Store
